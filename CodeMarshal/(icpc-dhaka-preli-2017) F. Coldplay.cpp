// In the name of Allah the merciful.
#include<bits/stdc++.h>
using namespace std;

int main(void)
{
    int p, q, y;
    cin >> p >> q >> y;
    int ans = (y * (52 * ((5 * p) + (2 * q))));
    cout << ans << endl;
    return 0;
}
